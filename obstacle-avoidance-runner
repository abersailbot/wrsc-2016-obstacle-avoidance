from multiprocessing import Process, Manager

def f(namespace):
    print(namespace)
    namespace.value = True

if __name__ == '__main__':
    with Manager() as manager:
        namespace = manager.Namespace()

        p = Process(target=f, args=(namespace,))
        p.start()
        p.join()

        print(namespace)
